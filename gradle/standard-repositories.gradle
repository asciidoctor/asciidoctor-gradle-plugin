repositories {
    jcenter()
    mavenCentral()
    if(version.endsWith('-SNAPSHOT')) {
        mavenLocal()

        repositories {
            maven { url "https://dl.bintray.com/jruby-gradle/plugins" }
        }
    }
}
