%div{:id=>@id, :class=>['quoteblock', role]}
  - if title?
    .title=title
  %blockquote=content
  - if (attr? :attribution) or (attr? :citetitle)
    .attribution
      - if attr? :attribution
        &#8212; #{attr :attribution}
      - if attr? :citetitle
        - if attr? :attribution
          %br
        %cite=attr :citetitle
